// Criando um vetor
let nomes = [];

// Adicionando 3 items no vetor
nomes.push("João"); // Posição 0
nomes.push("JOseh");
nomes.push("Jhonas com H");

// Alterar o valor no vetor
nomes[1] = "José";

// Apagar o 'Jhonas com H'
nomes.splice(2, 1);

// Quantidade de elementos do vetor
nomes.length;